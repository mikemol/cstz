{"id": "p-atoms-gap-preserving", "type": "principle", "title": "Atoms are gap-preserving", "body": "An atom is a predicate where firing = confidence, not-firing = gap. Not Boolean negation. 'Did not fire' is not evidence of absence; it is absence of evidence. This is what distinguishes the open-world Belnap semantics from the classical two-valued Boolean semantics the pipeline must avoid collapsing to prematurely.", "parents": [], "status": "active", "refs": ["paper:Def 9.8", "paper:§3"]}
{"id": "p-flat-k-pool", "type": "principle", "title": "One flat K pool, no families", "body": "Every K lives in a single undifferentiated pool. No kind/edge/source/token/cite family taxonomy at registration time. Families are an interpretation to be earned by residue-driven wedge discovery, not asserted up front. Every raw observable string is its own K; wedges go into the same pool as primitives; subsequent passes can wedge wedges without any family-of-origin constraint.", "parents": [], "status": "active", "refs": ["paper:§3", "paper:Thm 8.6"]}
{"id": "p-no-canonicalization", "type": "principle", "title": "No canonicalization at registration time", "body": "Do not lowercase, do not strip, do not map equivalent-looking strings to a canonical form. Any equivalence between observationally-distinct tokens ('FunctionDef' vs 'function', 'Module' vs 'module') must EMERGE from residue-driven wedge discovery, not be asserted by code. Canonicalization is hardcoded foreknowledge.", "parents": ["p-flat-k-pool"], "status": "active", "refs": []}
{"id": "p-no-bespoke-recognition", "type": "principle", "title": "No bespoke pattern recognition at any level", "body": "The pipeline recognizes nothing by hand — not kind tags, not source provenance, not arity, not type signatures, not axiom shapes, not triples. Every 'pattern I'd recognize' is a specific grade of wedge that residue-driven discovery should earn. Writing a pattern-matcher anywhere in the pipeline breaks self-hosting.", "parents": ["p-flat-k-pool", "p-atoms-gap-preserving"], "status": "active", "refs": ["paper:App B §B.39"]}
{"id": "p-arity-is-grade", "type": "principle", "title": "Arity = grade; arity N is not tracked, it emerges", "body": "Arity is the GRADE of the discovered wedge, not a variable counted and incremented. Arity-1 is a single atomic K. Arity-2 is a wedge of two grade-1's noticed via σ/τ correspondence. Arity-3 is a correspondence between an arity-2 and an arity-1. N is structurally evolved over iterated κ-evolution passes, not tracked in a schedule or counter.", "parents": ["p-no-bespoke-recognition"], "status": "active", "refs": ["paper:Thm 8.6"]}
{"id": "p-source-is-a-k", "type": "principle", "title": "Source provenance is just another K", "body": "The three-source distinction (paper/agda/python) is a hardcoded taxonomy not to be imposed at registration. The fact that a thing came from a particular parser is one observable K among many. A truly organic run pools every named object across all sources and discovers source-clustering as a K that correlates with some cluster centers; the three-way grouping is measured, not asserted.", "parents": ["p-flat-k-pool", "p-no-bespoke-recognition"], "status": "active", "refs": []}
{"id": "p-tau-sigma-separation", "type": "principle", "title": "τ/σ separation is load-bearing", "body": "τ and σ are two independent gap-preserving predicates forming the two channels of each discriminator. Collapsing them into a single bitmap loses the chirality that distinguishes Belnap four-cell from Boolean two-cell. τ/σ separation is required to even define residue meaningfully: without it, the discrimination direction is absent.", "parents": ["p-atoms-gap-preserving"], "status": "active", "refs": ["paper:§3 Def 3.1-3.6"]}
{"id": "p-tau-sigma-not-opposite", "type": "principle", "title": "τ/σ is not the opposite of σ/τ", "body": "The ordered pair (τ, σ) is not the inverse of (σ, τ). Pairing τ with σ is not enough to earn Boolean. They are two orientations of a chiral operation; testing both orderings probes each interpretation independently. The '/' in '(σ/τ)/(τ/σ)' is the separator between left and right columns in a four-valued logic matrix, not a ratio.", "parents": ["p-tau-sigma-separation"], "status": "active", "refs": []}
{"id": "p-boolean-earned-by-both-off-diagonals", "type": "principle", "title": "Boolean is earned when both off-diagonal cells are populated", "body": "For a pair of flags (σ, τ) to earn Boolean discrimination, the evidence distribution must populate BOTH the (σ-fires, τ-gaps) cell AND the (τ-fires, σ-gaps) cell. This proves σ and τ are neither redundant (always co-fire), implicative (one subsumes the other), nor vacuous (both silent). Boolean earning is an XOR-independence property of the pair of flags.", "parents": ["p-tau-sigma-separation", "p-atoms-gap-preserving"], "status": "active", "refs": []}
{"id": "p-alignment-is-distribution", "type": "principle", "title": "Alignment is a distribution, not a predicate", "body": "A pair's alignment signal is its Belnap four-cell profile per K-pair per grade — a four-valued distribution, not a binary verdict. Boolean-earning is one reading of that profile available for some pairs under some K-pairs, but it is not a commit gate. Pre-Boolean signal is valuable and must be preserved; every pair perpetually carries its evidence profile.", "parents": ["p-boolean-earned-by-both-off-diagonals", "p-atoms-gap-preserving"], "status": "active", "refs": []}
{"id": "p-closed-self-referential-loop", "type": "principle", "title": "Pipeline loop must be closed and self-referential", "body": "No external scheduler. No max_iters. No hand-placed stage boundaries between extraction/calibration/alignment/validation. Every seam where the author decides when to transition is external orchestration and violates closure. The correct form is a single step(state) → state' iterated to a fixed point, where fixed point = no productive wedge articulation changes the evidence distribution.", "parents": [], "status": "active", "refs": ["paper:§3 κ-evolution", "paper:App B §B.39"]}
{"id": "p-chirality-only-for-higher-valence", "type": "principle", "title": "Chirality semantics are optional at pair level", "body": "Interpretations of τ/σ that incorporate chirality (direction-sensitive combinators, quaternionic structure) are useful when combining more than two elements — triples, quaternions, higher. At the pair level they are optional and we are not ready for them yet. The pair-level rebuild starts without invoking chirality interpretations.", "parents": ["p-tau-sigma-separation"], "status": "active", "refs": []}
{"id": "p-iteration-count-unknown", "type": "principle", "title": "Convergence count is unknown and unpromiseable", "body": "If the pipeline is doing the algorithm correctly from the correct level of primitive, it is operating closer to universal constructions than the paper author. The number of κ-evolution passes before cross-source commutativity is discovered is unknown in principle. This is why the loop must be closed and self-referential — the author cannot orchestrate convergence externally.", "parents": ["p-closed-self-referential-loop", "p-no-bespoke-recognition"], "status": "active", "refs": []}
{"id": "p-maximal-freedom", "type": "principle", "title": "Prefer interpretations that leave the greatest remaining freedom", "body": "When choosing between design options, pick the one that makes the FEWEST commitments. Restrictive readings close doors; permissive readings keep them open. The wrong read is not 'the one that doesn't work' — it is 'the one that is unduly restrictive'. A framework that is maximally free is maximally composable, maximally flexible, and maximally able to absorb future corrections without structural rewrites. Operationally: parameterize rather than pick; implement algebra uniformly over formal channels rather than asserting what the channels observe; leave observational content as a population choice rather than a framework choice; default to the most symmetric initial state but permit asymmetric ones.", "parents": [], "status": "active", "refs": [], "session": "2026-04-15"}
{"id": "p-kappa-is-derived-xor", "type": "principle", "title": "κ = σ ⊕ τ is a derivation, not an independent axis", "body": "The third coordinate κ of the root S3 is determined by τ and σ via GF(2) addition: κ = σ ⊕ τ (equivalently the paper's χ = τ + σ from Def 3.6). The triple (τ, σ, κ) lives in F₂² under the linear constraint κ = τ + σ. S3 acts on {τ, σ, κ} by permuting which pair is treated as primary / which as derived — any two of the three determine the third. There is no preferred axis; the framework must respect this.", "parents": ["p-tau-sigma-separation", "p-tau-sigma-not-opposite"], "status": "active", "refs": ["paper:Def 3.6"], "session": "2026-04-15"}
{"id": "p-atoms-are-formal-tau-sigma-channels", "type": "principle", "title": "Atoms carry two independent formal channels; their content is a population choice", "body": "At the framework level an atom K assigns, per thing X, a pair (τ_K(X), σ_K(X)) ∈ F₂². What τ and σ observe OBSERVATIONALLY is not committed to by the framework — it is a population choice made by the atom extractor and may be refined by later articulations. κ_K(X) = τ_K(X) ⊕ σ_K(X) is derived. Default initial population: τ = σ = 'K fires on X', giving κ = 0 identically at grade 1 — the maximally symmetric state that leaves future asymmetry as a free move.", "parents": ["p-maximal-freedom", "p-kappa-is-derived-xor", "p-atoms-gap-preserving"], "status": "active", "refs": [], "session": "2026-04-15"}
{"id": "p-self-similarity", "type": "principle", "title": "The framework is self-similar at every scale and grade", "body": "The same structure, the same operations, the same interface at every level. Atoms and wedges are both K's — no special machinery distinguishes them in the algebra. step() at grade 1 is the same function as step() at grade N. State over a subset of things is a valid State; state over the full corpus is a valid State; they compose via set-union. Articulation is itself a step() operation, not a separate phase. This is the fractal / recursive structure that lets the framework be both maximally free AND embarrassingly parallel: any work cell can operate on any subset; merging work cells is concatenation.", "parents": ["p-maximal-freedom", "p-closed-self-referential-loop"], "status": "active", "refs": [], "session": "2026-04-15"}
{"id": "p-embarrassingly-parallel", "type": "principle", "title": "Work cells are mergeable by concatenation", "body": "Side benefit of self-similarity: the framework parallelizes trivially. A work cell is a step() invocation on a partition of state; partitions merge via set-union (Belnap cell counts sum; K pool unions; thing sets union). There is no coordination protocol, no locking, no special reducer. If N sessions each process a shard and append their output to shared JSONL files, a subsequent session can load the union and run step() once more to reconcile. The disk representation IS the merge protocol.", "parents": ["p-self-similarity"], "status": "active", "refs": [], "session": "2026-04-15"}
{"id": "p-type-theory-everywhere", "type": "principle", "title": "Use type theory to encode intensionality structurally", "body": "Types carry meaning.  A K is not a string key; it is an inductive type (Atom | Wedge(K, K)) whose structure IS its intensional identity.  A TauSigma is not a pair of ints; it is an F₂²-valued type with the κ = τ ⊕ σ constraint enforced at the type level.  An S3 element is not an enum value; it is a group-theoretic object whose action on types is defined by dependent-type parameterization.  Distinct semantic roles get distinct types even when bit-level representations coincide.  The type system is load-bearing — it prevents cross-category conflation that would otherwise erode the framework's self-similarity.", "parents": ["p-maximal-freedom", "p-self-similarity"], "status": "active", "refs": [], "session": "2026-04-15"}
{"id": "p-extensionality-via-hit", "type": "principle", "title": "Extensionality is earned through dependent types and HIT", "body": "Intensional equality (two K's equal iff identically-constructed) is the default.  Extensional equality (two K's equal because they produce the same behavior) is EARNED via quotient types / higher inductive types: path constructors declare 'these two intensionally-distinct things are the same in this extensional sense'.  S3 action on (τ, σ, κ) is expressed as path constructors (permutations of the triple that preserve the κ = τ ⊕ σ constraint).  Wedge commutativity Wedge(a, b) ≡ Wedge(b, a) is a HIT path constructor, not an ad-hoc normalization.  In Python, which lacks native HIT, these are simulated via explicit canonicalization functions and equivalence-class representatives — but the intent is HIT and the Python simulation is a pragmatic lowering.", "parents": ["p-type-theory-everywhere", "p-kappa-is-derived-xor"], "status": "active", "refs": [], "session": "2026-04-15"}
{"id": "p-bijective-hash-consing", "type": "principle", "title": "All hash-consing encoding schemes must be bijective", "body": "Any key, id, or canonical-representation string used to name or compare framework data must uniquely determine the data it names.  A cryptographic hash of source data is NOT bijective (it compresses N bits to ≤N and thus admits collisions).  An unescaped concatenation containing user-controlled separators is not bijective (f'wedge({a.key()},{b.key()})' becomes ambiguous if any observable contains a comma).  Acceptable forms: structured data (tuples), escape-safe serialization (JSON.dumps), positional data concatenated with verifiably-unique separators.  When in doubt, use a round-trippable serialization: the encoder and decoder must exist and round-trip to the identity.  Non-bijective encodings are a correctness bug, not an efficiency concern — two different things that hash-cons to the same key are one thing in the framework's view, which is wrong.", "parents": ["p-type-theory-everywhere"], "status": "active", "refs": [], "session": "2026-04-16"}
{"id": "p-yoneda-identity", "type": "principle", "title": "A thing's identity is its probe-firing profile (Yoneda)", "body": "Under the Yoneda lemma applied to the framework: a thing is determined by its relations to its peers, and in our parlance those relations ARE probe firings.  A thing's identity is precisely the set of probes for which the thing has an epistemological stance — whether that stance is 'fires' (confidence), 'gaps' (explicit absence of evidence), or later σ/τ-asymmetric positions.  Two things with identical probe profiles are indistinguishable in the framework.  Corollary: every piece of information that discriminates a thing from its peers MUST be expressed as a probe.  Info encoded only in opaque identity strings (e.g. path embedded in ThingId) is not part of the framework-visible identity and cannot participate in probe quotienting.", "parents": ["p-atoms-gap-preserving", "p-bijective-hash-consing"], "status": "active", "refs": [], "session": "2026-04-16"}
{"id": "p-probes-are-half-spaces", "type": "principle", "title": "Probes define half-spaces over the thing-pool; quotienting projects to cells", "body": "Each probe P partitions the thing-pool into two parts: things where P fires (the probe's half-space, or 'hyperdemiplane' / 'ray') and things where P gaps (no evidence, NOT the classical complement).  An arrangement of probes creates cells by intersection; these cells are the framework's native granularity for subset resolution.  Probe-quotienting = projection to a chosen cell (or union of cells).  Constraint application = selecting the cell matching a constraint pattern.  The algebraic arena (exterior algebra over GF(2)) is where probe bitmaps compose via wedge products, giving a compositional language for cell selection.  Design rule: register probes that carve the thing-space along semantically-meaningful dimensions; do not register individual things as probes (each thing is a POINT in the space, not a half-space).", "parents": ["p-yoneda-identity", "p-flat-k-pool", "p-bijective-hash-consing"], "status": "active", "refs": [], "session": "2026-04-16"}
{"id": "p-thing-is-not-its-relations", "type": "principle", "title": "Things with relations are distinct things, not internal fields", "body": "If two somethings can be semantically distinguished, they are two separate things, even when a relation exists between them.  A decl's docstring is NOT part of the decl's identity — the docstring is its own thing, with its own probe profile, related to the decl by an 'attached-to' relation.  Extraction should therefore register separate Things for each distinguishable object (decl, docstring, type signature, comment, etc.) and express their relations as probes / wedges rather than as internal fields of one thing.  This keeps the algebra compositional: relations between things are first-class objects quotientable by probe arithmetic.", "parents": ["p-yoneda-identity", "p-source-is-a-k"], "status": "active", "refs": [], "session": "2026-04-16"}
{"id": "p-four-cell-xor-score", "type": "principle", "title": "Four-cell score is XOR of columns; only off-diagonals contribute", "body": "For a K-pair (K_i, K_j) with 2×2 joint-firing counts (n_00, n_01, n_10, n_11) across things, the K-pair's score is the XOR of the two columns — the count of things where exactly one K fires.  Only the off-diagonal cells (n_01 and n_10) contribute.  Gap-gap (n_00) and both-fire (n_11) cells contribute NOTHING to the score.  Rationale: gap-gap is absence of evidence (p-atoms-gap-preserving), and both-fire is redundant evidence that already aligns the two K's on a set of things — neither is discriminating evidence.  Only XOR firings discriminate.  This is the scoring expression of p-boolean-earned-by-both-off-diagonals: each off-diagonal observation is a witness that K_i and K_j genuinely pull apart some thing's identity.", "parents": ["p-atoms-gap-preserving", "p-boolean-earned-by-both-off-diagonals", "p-alignment-is-distribution"], "status": "active", "refs": [], "session": "2026-04-16"}
{"id": "p-overlap-demands-wedge-articulation", "type": "principle", "title": "Overlap cell (n_11 > 0) demands wedge articulation when the joint is uncaptured", "body": "Although the both-fire cell (n_11) contributes zero to the score of a K-pair (p-four-cell-xor-score), it has a distinct role: it is the DETERMINISTIC TRIGGER for wedge articulation.  When a K-pair has n_11 > 0 — some thing fires both K's — AND no existing K in the pool already captures the joint firing pattern K_i AND K_j unambiguously (i.e., no K whose firing bitmap equals the bitwise AND of K_i's and K_j's firing bitmaps), the pool is under-articulated with respect to this joint.  The articulation is demanded: step() must register K_i ∧ K_j as a new K to restore representational sufficiency.  The 'overdetermined state' exception: if an existing K already captures the joint exactly, adding another wedge would be redundant (the articulation would map to the same extensional representative).  Scorers can still RANK articulation candidates (for ordering within a step), but the rule for WHETHER to articulate is not scorer-based — it is a bijective property of the current pool against the current thing-pool.", "parents": ["p-four-cell-xor-score", "p-arity-is-grade", "p-extensionality-via-hit", "d-wedge-bit-and-of-parents"], "status": "active", "refs": [], "session": "2026-04-16"}
{"id": "p-functions-have-structural-identity", "type": "principle", "title": "Functions used as plug-ins / visitors carry structural identity (no anonymous closures)", "body": "Every function used as a scorer, objective, articulation-rule, or any other framework-level continuation MUST have a structural identity — a bijective representation of what it computes, independent of closure capture.  Callable objects are frozen hashable dataclasses with an explicit ``structure`` property returning a tuple that (together with any captured configuration values) uniquely identifies the function's effect.  This is the 'continuance' pattern: the function's behavior is reproducible from its structural representation alone.  Anonymous closures are forbidden; they lack structural identity and cannot survive serialization (pickling across process boundaries per p-embarrassingly-parallel), trajectory logging (recording which scorer proposed which wedge), or cross-session merging.  In visitor-pattern terms: a scorer is a static function applied over a collection, and any function has an AST-shaped measurable structure — that measurable structure is what lets composites remain identifiable when they cross scope boundaries.", "parents": ["p-bijective-hash-consing", "p-embarrassingly-parallel", "p-type-theory-everywhere"], "status": "active", "refs": [], "session": "2026-04-16"}

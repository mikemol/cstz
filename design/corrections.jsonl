{"id": "c-arity-not-a-feature", "type": "correction", "title": "Arity is grade, not a counted feature", "body": "Corrected my proposal to register 'arity/N' as a primitive K. The correct framing: arity is the grade of a wedge discovered via σ/τ correspondence between grade-(N-1) and grade-1 (or grade-N-1 things). N is not tracked in a variable; it is structurally evolved via iterated wedge passes. This correction supersedes the 'register arity as primitive' design direction.", "parents": ["r-hardcoded-arity-k", "p-arity-is-grade"], "status": "active", "session": "2026-04-15"}
{"id": "c-lowercase-still-hardcoded", "type": "correction", "title": "Lowercasing is hardcoded equivalence, not a generic transform", "body": "I had framed the lowercase normalization as 'a generic casing normalization, not a hand-picked mapping'. The user corrected that this is still a hardcoded mapping at a higher level. Reverted. The correct rule is strictly no canonicalization: all observational distinctions are preserved verbatim at registration.", "parents": ["r-lowercase-canonicalization", "p-no-canonicalization"], "status": "active", "session": "2026-04-15"}
{"id": "c-bespoke-pattern-recognition-rejected", "type": "correction", "title": "Even 'elaborate subtree matching for specific axioms' must emerge", "body": "I briefly proposed writing pattern-matchers for axiom structures. User corrected this must ALSO be emergent, not bespoke. The principle extends to every level: no recognizer for any specific pattern (not kinds, not arity, not axioms, not source-clusters, not triples) may be hand-written. All recognition is κ-articulated.", "parents": ["r-bespoke-axiom-detectors", "p-no-bespoke-recognition"], "status": "active", "session": "2026-04-15"}
{"id": "c-alignment-is-distribution-not-gate", "type": "correction", "title": "Alignment is the evidence distribution itself, not a Boolean gate", "body": "I proposed interpretation (b): alignment = pair demonstrates Boolean across some K-pair. User corrected: this is still a commit gate in disguise. Pre-Boolean evidence is valuable signal — κ-evolution FEEDS on it; it is not a waystation on the road to Boolean. Boolean-earning is an occasional reading of the Belnap distribution, not its point. Commit predicates at any level are out.", "parents": ["r-commit-as-boolean-earned", "p-alignment-is-distribution"], "status": "active", "session": "2026-04-15"}
{"id": "c-division-notation-is-matrix-cell", "type": "correction", "title": "'/' in (σ/τ) is matrix-cell separator, not ratio", "body": "I interpreted ((σ/τ)/(τ/σ)) as a division / ratio / Jaccard-style operator. User corrected: each '/' separates left from right columns in a four-valued logic matrix. Boolean is earned when a flag fires while the other does not AND the other flag fires while the first does not — both off-diagonal cells populated. This is XOR-independence, computed trivially as two cell counts both > 0.", "parents": ["p-boolean-earned-by-both-off-diagonals"], "status": "active", "session": "2026-04-15"}
{"id": "c-context-externalized-to-sppf", "type": "correction", "title": "Session context externalized to design/ SPPF", "body": "Sessions kept hitting context limits trying to carry the full history of principles + decisions + corrections + code state in one working set. Moved the design history into design/ as JSONL files walkable by scripts/design_sppf.py. Future sessions load only the slice they need (specific principles, dependents of a decision, open questions) rather than the conversation.", "parents": ["p-closed-self-referential-loop"], "status": "active", "session": "2026-04-15"}
{"id": "c-question-resolution-via-maximal-freedom", "type": "correction", "title": "Open questions resolve via maximal freedom, not specific picks", "body": "I was treating the five open questions (q-tau-sigma-wedge-combinator, q-wedge-articulation-signal, q-weight-gradient-direction, q-report-format-for-belnap-distribution, q-smoke-test-scale) as blockers needing concrete user decisions. Under p-maximal-freedom they collapse to a single non-answer: don't pick, parameterize. Implement the algebra uniformly; make scorers, objectives, and views pluggable; default to the most symmetric initial state; allow asymmetry as a free move at higher grades. Each question is thus closed by deferral-to-framework rather than resolved by commitment.", "parents": ["p-maximal-freedom", "q-tau-sigma-wedge-combinator", "q-wedge-articulation-signal", "q-weight-gradient-direction", "q-report-format-for-belnap-distribution", "q-smoke-test-scale"], "status": "active", "session": "2026-04-15"}
{"id": "c-metadata-was-a-smuggled-taxonomy", "type": "correction", "title": "Metadata class was a smuggled taxonomy, removed in Stage 2.6", "body": "Stage 2.5 introduced a Metadata dataclass (display, source_path, line, extra) as a 'separate informational-only struct the algebra never inspects'.  User pointed out: 'what even is metadata in this context?'  On reflection, Metadata as a category is itself a privilege the framework shouldn't grant.  Under p-source-is-a-k and p-no-bespoke-recognition, there is no reporting-vs-structure dichotomy: every observable is a K.  If an extractor wants display labels or source paths or line numbers visible to reports, it registers them as atomic K's (e.g. Atom('source:paper/x.py')) that fire on the Thing's tau bitmap.  Reports are queries over the pool + bitmaps, not queries over a separate metadata dict.  Stage 2.6 removes the Metadata class, the State.metadata field, metadata_of/metadata_dict accessors, and all metadata handling in merge/restrict.  Thing is now strictly (ThingId, tau_mask, sigma_mask).", "parents": ["p-source-is-a-k", "p-no-bespoke-recognition", "p-flat-k-pool"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-3-5-bijective-keys-and-stable-anon-ids", "type": "correction", "title": "Stage 3.5: bijective K keys; stable position-based anon-thing-IDs; surfaced parse errors", "body": "Stage 3 audit found three smells that combine to violate the newly-explicit p-bijective-hash-consing principle. Fix: (1) K.key() now derives from K.structure() — a bijective tuple serialized via JSON. ('atom', observable), ('wedge', left.structure(), right.structure()), ('zero',). JSON encoding is round-trippable; observables containing commas or parens no longer produce ambiguous wedge keys. (2) Anonymous-thing IDs now use (source_path, source_line) positional disambiguation instead of an extraction-order counter. Every adapter (PandocNode, TreeSitterAgdaNode, PyAstNode) already exposes source_path and source_line. Collision detection: extract_initial_state raises ValueError if two Things get the same ID (would indicate line-level collision requiring byte-level disambiguation). (3) Parse failures are logged to stderr (per-file) rather than silently swallowed. They remain non-fatal for corpus-wide extraction but are now visible.", "parents": ["p-bijective-hash-consing", "p-embarrassingly-parallel", "p-closed-self-referential-loop"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-3-6-identity-as-probes", "type": "correction", "title": "Stage 3.6: surface all identity components as probes, not just as ID-string text", "body": "Stage 3 audit under the newly-explicit p-yoneda-identity found that Thing identity components (source, top-level kind, path, name) were encoded only as opaque text in ThingId strings.  This violates p-yoneda-identity (identity = probe profile) and p-probes-are-half-spaces (discriminable info must live in the probe algebra).  Stage 3.6 expands the initial atom pool to include: kind_at_root:{kind} (distinct from subtree-kind probes), path:{component} per path fragment, name:{verbatim} per named thing's verbatim name.  NOT added: line numbers (positional disambiguation only, not compositionally meaningful yet), name-token splits (canonicalization, forbidden by p-no-canonicalization), docstring content (docstrings are their own things per p-thing-is-not-its-relations).  Scale: ~145 → ~1100+ atoms; still well within bitmap feasibility.", "parents": ["p-yoneda-identity", "p-probes-are-half-spaces", "p-thing-is-not-its-relations"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-4-5-xor-score-and-overlap-demand", "type": "correction", "title": "Stage 4.5: four-cell scoring is XOR-of-columns; overlap cell demands articulation", "body": "Stage 4's scorer_entropy_of_four_cell computed Shannon entropy across all four cells including n_00 (gap) and n_11 (overlap), conflating gap-as-absence with overlap-as-redundancy under a single categorical-distribution reading.  User specified the correct semantics: the score of a four-cell is the XOR of the two columns — only off-diagonals score.  n_00 is gap (zero score, zero trigger).  n_11 has zero score but is the deterministic trigger for wedge articulation when the joint is not yet in the pool.  Stage 4.5 adds scorer_xor_off_diagonal as the principled primary scorer (n_01 + n_10); retains scorer_entropy_of_four_cell as a general-distribution statistic for reporting / characterization but not as a ranking default.  Also adds scorer_xor_off_diagonal_log_pair for the Boolean-earning variant (log(n_01) + log(n_10) when both populated).  The overlap-demand rule (p-overlap-demands-wedge-articulation) will be enforced in Stage 5's step() function.", "parents": ["p-four-cell-xor-score", "p-overlap-demands-wedge-articulation"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-4-6-structural-functions-plus-split-orientations", "type": "correction", "title": "Stage 4.6: structural scorer/objective classes; split two-orientation oracle scorer; decomposed signatures", "body": "Stage 4 used bare Callable type aliases and closure-based composites.  Under p-functions-have-structural-identity (added this session) closures must become frozen dataclasses with explicit ``structure`` properties.  Stage 4.6 delivers: (1) Scorer and Objective as Protocol classes with decomposed named-parameter signatures; ScoreValue and ObjectiveValue as NewType float wrappers for semantic decomposition.  (2) Each scorer refactored from bare function to frozen dataclass with __call__ and structure.  Singleton instances exposed under the old function names for call-site continuity.  (3) CompositeScorer and CompositeObjective as frozen dataclasses containing tuple of (weight, component) pairs; identity is structural (recursive tuple).  (4) ``scorer_oracle_boolean_witness`` split into two orientation-specific scorers (tau-sigma vs sigma-tau) per p-tau-sigma-not-opposite.  Union variant provided as a composite for callers who want the sum.  (5) ``import math`` moved to module top.  Per-K firing-bitmap precomputation deferred to Stage 5 (efficiency concern, not Stage 4.6 scope).", "parents": ["p-functions-have-structural-identity", "p-tau-sigma-not-opposite", "p-bijective-hash-consing"], "status": "active", "session": "2026-04-16"}
{"id": "c-weight-updater-becomes-new-k-articulation", "type": "correction", "title": "WeightUpdater stub removed; weight updates reinterpreted as new-K articulation", "body": "Stage 5 scaffolded a pluggable WeightUpdater protocol with a NoOpWeightUpdater default per d-weight-objective-pluggable.  User clarified that weight updates as a concept destroy information — the mutation replaces the old scalar and loses the regime in which earlier wedges were articulated.  The honest alternative is p-weight-update-is-new-k-articulation: instead of modifying a K's weight, articulate a NEW K with the same observable and a different scalar.  Both coexist.  Stage 5.5 removes the WeightUpdater parameter from step(), removes NoOpWeightUpdater and the WeightUpdater Protocol, and leaves the State.weights field in place (now set-once per K at articulation time, not mutated by step()).  d-weight-objective-pluggable is superseded by p-weight-update-is-new-k-articulation in its 'weights tune toward objective' reading; the 'objectives exist' part remains valid as read-out and K-articulation-driver signal.", "parents": ["p-weight-update-is-new-k-articulation", "d-weight-objective-pluggable"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-5-5-no-default-budget-remove-weight-updater", "type": "correction", "title": "Stage 5.5: no-default articulation budget; WeightUpdater removed; σ channel exercised", "body": "Stage 5 audit identified four addressable concerns.  Stage 5.5 fixes: (1) max_articulations_per_step default changes from 100 to None meaning 'all demanded wedges articulated in one step'.  Bounded budgets become a caller choice, not a framework default — matching p-maximal-freedom (don't pick) and p-closed-self-referential-loop (no external schedule baked in).  (2) WeightUpdater removed per c-weight-updater-becomes-new-k-articulation.  (3) _articulate_wedge_into_state docstring updated with the 'wedge of appearance vs reality' pattern: explicit note that the function APPEARS pure but MUTATES step-local caches as an incrementalization strategy scoped to the batch.  (4) σ-channel path exercised in smoke test: _compute_firing_bitmaps called with channel='sigma' and result compared to the τ path; under the current τ=σ regime they agree, but the path itself is tested.", "parents": ["p-weight-update-is-new-k-articulation", "p-maximal-freedom", "p-closed-self-referential-loop"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-eager-numpy-refactor", "type": "correction", "title": "Stage 7.0: eager numpy refactor of internal representation", "body": "Thing.tau_mask and sigma_mask are Python ints (arbitrary-precision bitmasks).  Stage 7.0 converts them to np.ndarray(dtype=bool) throughout.  Affected surface: Thing (new __post_init__ for array immutability; __hash__ via .tobytes(); new remap() using numpy index-transform), _compute_firing_bitmaps (returns 2D bool array [pool_size, n_things] instead of dict[int, int]), _count_four_cell (numpy bitwise ops, sum for popcount), step() (batched articulation — all new wedges computed as 2D firing matrix, concatenated onto every thing's mask once per batch instead of once per wedge), extract_initial_state (constructs Things with np arrays from the start).  Signature() continues to work via Thing.__hash__ which uses .tobytes() for deterministic content hashing.  Smoke tests adjusted for numpy-array assertions.  Under p-refactor-debt-paid-eagerly: the scope is manageable now (1087 atoms × 854 things), and paying now prevents the surface from growing through Stages 7.1+.", "parents": ["p-numpy-is-the-natural-cpu-representation", "p-refactor-debt-paid-eagerly", "p-storage-matches-data-shape"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-1-hybrid-jsonl-hdf5-io", "type": "correction", "title": "Stage 7.1: hybrid JSONL + HDF5 I/O with generator run()", "body": "Stage 7.1 delivers persistence: pool/trajectory/oracle as append-only JSONL; per-thing τ/σ bitmaps as HDF5 datasets with resizable axes (thing count + pool bit count).  dump_state(state, out_dir) writes both.  load_state(in_dir) reconstructs the State.  Generator form of run_to_fixed_point (run_iter) yields State at each iteration for progress inspection / interruptible runs / automatic per-iteration checkpoint — addressing the Stage 6 audit's ergonomics concern.  Refines d-report-is-full-state-jsonl via p-storage-matches-data-shape: the 'full state queryable' intent stands; the 'single-format JSONL' implementation is now hybrid.", "parents": ["p-storage-matches-data-shape", "d-report-is-full-state-jsonl", "p-numpy-is-the-natural-cpu-representation"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-5-apply-numpy-not-just-store", "type": "correction", "title": "Stage 7.0.5: apply numpy arrays array-natively, not via Python loops", "body": "Stage 7.0 installed np.ndarray as the representation but left three inner-loop code paths in Python: (1) _count_four_cell looped per-thing and appended to Python lists before converting to numpy; (2) step()'s candidate-pair enumeration did a Python C(n, 2) loop per thing × n_things things; (3) Thing.__hash__ re-hashed .tobytes() every call, compounding through signature() twice per iteration in run_to_fixed_point.  Under p-numpy-is-the-natural-cpu-representation the principle says 'arrays for application to other arrays' — arrays stored but not applied is neither-fish-nor-fowl.  Stage 7.0.5: (1) _count_four_cell accepts optional firing_bitmaps for fast path via 2D matrix slicing + np.sum; Scorer protocol extends with optional firing_bitmaps kwarg so scorers can take the fast path when called from step(); (2) step() candidate enumeration uses firing_bitmaps @ firing_bitmaps.T to build a [pool_size × pool_size] co-fire count matrix, then extracts upper-triangle pairs where count > 0 — O(P²·N) BLAS matmul replaces Python O(P·N·bits²); (3) Thing.__hash__ caches the computed hash via object.__setattr__ on first call, read via getattr on subsequent.  No principle change; all active principles preserved.", "parents": ["p-numpy-is-the-natural-cpu-representation", "p-refactor-debt-paid-eagerly"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-6-unmaterialize-hash-and-signature", "type": "correction", "title": "Stage 7.0.6: unmaterialize Thing._hash and replace signature()-based fixed-point with trajectory signal", "body": "User observation: 'we're materializing something that shouldn't be materialized in the first place'.  Stage 7.0.5 added Thing._hash caching to optimize signature() comparison in run_to_fixed_point.  But signature() returns a tuple; tuple == tuple uses element-wise __eq__, not hash().  Thing.__hash__ is never invoked during fixed-point detection.  The cache solved a phantom problem AND introduced cross-process staleness for no benefit.  Stage 7.0.6 reverts the cache: Thing.__hash__ returns hash((id, shape, tobytes(), tobytes())) on every call, pure function of content; no state, no cross-process issue.  Cascading: run_to_fixed_point no longer computes or compares signatures.  Termination = state.trajectory[-1]['articulated_count'] == 0.  Direct signal from the trajectory (which step() already writes), exact match for d-fixed-point-is-termination's 'no productive wedge articulation' criterion.  signature() itself is retained for diagnostic / external equality checks (smoke-test assertions, comparison across saved states) but is no longer in the closed-loop hot path.", "parents": ["p-numpy-is-the-natural-cpu-representation", "p-closed-self-referential-loop", "d-fixed-point-is-termination"], "status": "active", "session": "2026-04-16"}
{"id": "c-type-theory-reframed-via-tensor-shape", "type": "correction", "title": "p-type-theory-everywhere reframed: primary focus is tensor shape, not class shape", "body": "User observation: 'Instead of a type making assertion about the obligations of the shape of a class, it should make assertions about the obligations of the shape/properties of a tensor.'  p-type-theory-everywhere was originally phrased around class-shaped types (K as inductive, TauSigma as F₂², Scorer as Protocol).  Stage 7's eager numpy refactor moved the algebra to tensor-valued operation; the primary type information shifted with it but the principle's body didn't follow.  c-type-theory-reframed-via-tensor-shape adds p-types-are-tensor-shape-obligations to carry the tensor-shape reading; p-type-theory-everywhere retains its class-shaped reading as secondary scaffolding.  Practical consequence for Stage 7.1: TrajectoryEntry is NOT a frozen dataclass — it's a row of a structured-dtype numpy array; the dtype schema IS the type.  Thing's tau_mask / sigma_mask should gain shape annotations (Bool[Array, 'pool_size']).  firing_bitmaps, ScoreValue, and other tensor-valued quantities similarly get shape-typed.", "parents": ["p-types-are-tensor-shape-obligations", "p-type-theory-everywhere", "p-numpy-is-the-natural-cpu-representation"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-7-three-lemma-bundle", "type": "correction", "title": "Stage 7.0.7: Pool + State.things + hash-consing refactored to numpy-native shapes (three-lemma bundle)", "body": "Stage 7.0.7 enacts three coupled lemmas as one refactor cycle: (a) l-pool-as-structured-dtype-array in 7.0.7a — Pool's authoritative representation becomes a 1-D structured-dtype numpy array (POOL_DTYPE = [('key','O'),('grade','u1')]) with a _key_to_bit dict cache for O(1) lookup; public .keys and .by_key properties preserve legacy shape for unchanged callers; (b) l-state-things-as-parallel-arrays in 7.0.7b — State stores thing_ids + tau_masks + sigma_masks as parallel 1-D/2-D arrays; 'a Thing' becomes a row-view reconstructed on demand via the .things property; merge's pool-remap is vectorized via advanced indexing; firing_bitmaps_of(state) returns state.tau_masks.T as an O(1) view, retiring the O(n_things · pool_size) _compute_firing_bitmaps rebuild in step()'s hot path; (c) l-hash-consing-as-np-unique in 7.0.7c — wedge-batch dedup replaces the Python seen_in_batch set with np.unique on the canonical keys array, making the Stage 7.0 double-registration bug a shape-level property rather than a maintained invariant.  The three lemmas bind a single dependent-type shape parameter 'pool_size' across axis-1 of the mask matrices and axis-0 of the pool structured array — one shape contract, three arrays, enforceable at load.  Together they enact p-types-are-tensor-shape-obligations on the largest in-memory shapes in the framework, preparing the ground for Stage 7.1's HDF5 compound-dtype schema (l-hdf5-compound-dtypes-mirror-in-memory) which will mirror these in-memory dtypes field-for-field.  No principle changes; all active principles preserved.  Smoke test passes at every substage (854 things, 1087 atoms, 107 oracle pairs; fixed-point trajectories identical to 7.0.6).", "parents": ["p-types-are-tensor-shape-obligations", "p-numpy-is-the-natural-cpu-representation", "p-refactor-debt-paid-eagerly", "l-state-things-as-parallel-arrays", "l-pool-as-structured-dtype-array", "l-hash-consing-as-np-unique"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-8-oracle-indices-and-shim-cleanup", "type": "correction", "title": "Stage 7.0.8: oracle_pair_indices + scorer vectorization + dead-code shims retired", "body": "Stage 7.0.8 enacts l-oracle-pairs-as-index-array and resolves the Stage 7 audit's Severity 1 + Severity 2 findings in one bundle.  (1) oracle_pair_indices: a new cached field on State, built at __init__ as Int[n_pairs, 2] by resolving the authoritative frozenset[frozenset[ThingId]] form against the _id_to_row dict.  Pairs sorted within-pair by (min, max) id-string, making orientation-sensitive scorers deterministic (previously they consumed frozenset iteration order, which was arbitrary in CPython and produced individually nondeterministic counts — though the sum across both orientations was invariant).  (2) state.row_of(tid) / tau_row(tid) / sigma_row(tid): O(1) row accessors backed by a _id_to_row dict cache.  Replace state.things_dict()[tid] for mask-row access — no Thing reconstruction, no per-row copy, no O(n_things · pool_size) rebuild.  (3) Four scorer/objective hot paths (OracleBooleanWitnessTauSigmaScorer, OracleBooleanWitnessSigmaTauScorer, OraclePairsWithWitnessObjective, PoolEntropyMarginalsObjective) vectorized to index tau_masks by oracle_pair_indices and reduce with numpy; no dict(state.things) calls remain in hot paths.  (4) Pool.key_to_bit exposed as a public property; internal call sites no longer reach into Pool._key_to_bit.  (5) Thing.remap removed — State.merge has vectorized the pool-remap as column-permutation since 7.0.7b; remap() had no framework callers post-refactor.  (6) _compute_firing_bitmaps (legacy shim) removed; three smoke-test call sites migrated to firing_bitmaps_of(state, channel).  REWRITE_STAGES.md updated to reflect the actual 7.0/7.1 split and all 7.0.x substages.  Smoke test passes (854 things, 1087 atoms, 107 oracle pairs); new assertions check oracle_pair_indices shape/dtype/range and row_of round-trip consistency.  No principle changes; all active principles preserved.", "parents": ["p-numpy-is-the-natural-cpu-representation", "p-types-are-tensor-shape-obligations", "p-refactor-debt-paid-eagerly", "l-oracle-pairs-as-index-array", "l-state-things-as-parallel-arrays"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-9-trajectory-structured-dtype-and-thing-docstring", "type": "correction", "title": "Stage 7.0.9: trajectory → structured-dtype array + aux; Thing docstring realigned with post-7.0.7b role", "body": "Stage 7.0.9 enacts l-trajectory-as-structured-dtype-array and closes the Stage 7 audit's remaining Severity 2 finding.  (1) TRAJECTORY_DTYPE (7-field structured numpy dtype) becomes the authoritative trajectory representation; field names are part of the type, so typos at write sites fail dtype validation rather than silently returning the .get() default.  max_articulations_per_step uses signed i4 with -1 as the sentinel for Python None — pure numeric, HDF5-ready.  (2) trajectory_aux is a parallel Tuple[dict, ...] carrying optional scorer/objective structural identity per the lemma's counterexample mitigation (split mandatory numeric fields from variable-shape aux fields).  (3) run_to_fixed_point termination check now reads state.trajectory[-1]['articulated_count'] as a structured-array field — no .get() default fallback.  (4) merge uses stable argsort on the 'iteration' field for lock-step sort of structured array + aux tuple; deterministic order.  (5) Legacy tuple-of-dicts trajectory kwarg still accepted at __init__ and normalized at construction.  (6) Thing docstring rewritten: Thing is a VALUE TYPE for standalone construction / row-view reconstruction, NOT the primary storage — State.tau_masks / sigma_masks is authoritative per l-state-things-as-parallel-arrays.  Points callers at state.row_of / tau_row / sigma_row for hot-path access.  Smoke test updated: scorer/objective structural-identity assertions moved from trajectory[-1] to trajectory_aux[-1]; max_articulations_per_step == _MAX_ART_UNBOUNDED_SENTINEL instead of `is None`; new assertion on last.dtype == TRAJECTORY_DTYPE.  Passes end to end (854 things, 1087 atoms, 107 oracle pairs).  No principle changes beyond the addition of p-set-algebra-and-tensor-reduction-use-distinct-forms; all prior active principles preserved.", "parents": ["p-types-are-tensor-shape-obligations", "p-set-algebra-and-tensor-reduction-use-distinct-forms", "p-numpy-is-the-natural-cpu-representation", "l-trajectory-as-structured-dtype-array", "l-state-things-as-parallel-arrays"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-10-scorer-cell-extractor-reducer-factoring", "type": "correction", "title": "Stage 7.0.10: five scorer classes factored into CellScorer(cells, reduce) compositions", "body": "Stage 7.0.10 enacts l-scorer-as-shape-contract.  The five original scorer classes (XorOffDiagonalScorer, XorOffDiagonalLogPairScorer, EntropyOfFourCellScorer, OracleBooleanWitnessTauSigmaScorer, OracleBooleanWitnessSigmaTauScorer) collapse to singleton compositions over two orthogonal structural pieces: (a) CellExtractor subclasses (ThingsFourCell, OracleSixteenCell) that map (state, i, j) to a cell count-tensor; (b) Reducer subclasses (SumOffDiagonal, LogProductOffDiagonal, ShannonEntropy, SelectCell(idx)) that reduce a cell tensor to a scalar.  CellScorer(cells, reduce) composes the two; structure tuple recurses as ('scorer', 'cell_scorer', cells.structure, reduce.structure) preserving intensional identity.  OracleSixteenCell exposes all 16 oracle-pair cells — previously 14 were inaccessible without writing a new class; now they cost a SelectCell(idx=...) binding.  Singletons written via functools.partial currying: on_things = partial(CellScorer, ThingsFourCell()); on_oracle_pairs = partial(CellScorer, OracleSixteenCell()); then e.g. scorer_xor_off_diagonal = on_things(SumOffDiagonal()).  Reads 'reducer on domain'; makes the (domain, reduction) factoring visible at the binding site.  Net LOC: -78 for scorers; adding any future scorer in the 2 × 4 Cartesian space now costs a binding, not a class.  Smoke test preserves all previous scorer assertions (tests exercise each singleton directly) and adds a fresh-composition equivalence check proving CellScorer holds no hidden state beyond (cells, reduce).  Composite structure assertion updated to the new ('scorer', 'cell_scorer', ...) format.  Objectives (OraclePairsWithWitnessObjective, PoolEntropyMarginalsObjective) and Composite{Scorer,Objective} unchanged — out of scope.  No principle changes; all active principles preserved.", "parents": ["p-types-are-tensor-shape-obligations", "p-functions-have-structural-identity", "p-maximal-freedom", "l-scorer-as-shape-contract", "l-oracle-pairs-as-index-array"], "status": "active", "session": "2026-04-16"}
{"id": "c-stage-7-0-11-count-four-cell-vectorized-int-signature", "type": "correction", "title": "Stage 7.0.11: _count_four_cell vectorized off state.tau_masks; signature takes bit indices", "body": "Stage 7.0.11 closes the post-7.0.10 Stage 7 audit's Severity 1 regression and Severity 2 K-object-roundtrip.  (1) _count_four_cell's slow path (firing_bitmaps=None) previously iterated state.things, which under post-7.0.7b reconstructs n_things Thing objects with per-row mask copies on every access.  Hot path was fine (step() always provides firing_bitmaps) but any direct scorer call paid O(n_things · pool_size) reconstruction.  7.0.11 replaces the iteration with state.tau_masks[:, i] / [:, j] column slices — O(1) view, no iteration, no Thing reconstruction.  (2) Signature changed from (state, k_i, k_j, channel, firing_bitmaps) to (state, i: int, j: int, *, firing_bitmaps=None, channel='tau').  Callers resolve K → bit_of upfront once; the Protocol-level guard handles missing-K cases before entry.  ThingsFourCell.__call__ drops its state.pool.ks[] roundtrip — it already has the ints.  (3) Four smoke-test callers updated to resolve bit indices upfront.  (4) New smoke-test assertion: slow-path output equals a direct state.tau_masks reference computation, proving the vectorization preserves semantics.  The outer CellScorer.__call__ still accepts K objects (preserves Scorer Protocol); Severity 2's 'step() passes K objects to scorer when it already has ints' remains open — that's a Protocol-level concern touching every Scorer contract and is deferred to a future stage.  Smoke test passes (854 things, 1087 atoms, 107 oracle pairs); all 5 CellScorer singletons bit-identical.  No principle changes; all active principles preserved.", "parents": ["p-numpy-is-the-natural-cpu-representation", "p-types-are-tensor-shape-obligations", "p-refactor-debt-paid-eagerly", "l-state-things-as-parallel-arrays", "l-scorer-as-shape-contract"], "status": "active", "session": "2026-04-16"}
